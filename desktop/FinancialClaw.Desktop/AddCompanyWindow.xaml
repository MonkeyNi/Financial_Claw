<Window x:Class="FinancialClaw.Desktop.AddCompanyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Add Company"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add Company" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1"
                   Text="Create the company workspace folders for source reports and processed outputs."
                   Foreground="{StaticResource MutedTextBrush}"
                   TextWrapping="Wrap"
                   Margin="0,6,0,16"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Company name" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBox x:Name="CompanyNameTextBox" Height="34"/>
            <TextBlock x:Name="ErrorText" Foreground="#B42318" Margin="0,8,0,0" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <ui:Button Content="Cancel" Appearance="Secondary" Style="{StaticResource TopCommandButtonStyle}" Click="CancelButton_Click"/>
            <ui:Button Content="Create" Appearance="Primary" Style="{StaticResource TopCommandButtonStyle}" Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
