Place the Windows application icon here:

```text
financial-claw.ico
financial-claw.source.png
```

The WPF project is already configured to use `Assets\financial-claw.ico` as the executable and window icon.
Keep `financial-claw.source.png` as the editable source image, and export a valid `.ico` before building.
