## MinerU Open API
#
# - Flash mode does NOT require a token (IP rate-limited).
# - Precision mode requires a token.
#
# Usage:
#   export $(cat mineru/.env | xargs)  # or use your own dotenv loader
#
MINERU_API_TOKEN=YOUR_MINERU_TOKEN_HERE

